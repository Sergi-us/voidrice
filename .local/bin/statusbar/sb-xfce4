#!/bin/sh
# XFCE Power Manager Settings

case $BLOCK_BUTTON in
	1) xfce4-power-manager-settings ;;
	3) notify-send "XFCE Power Manager" "Links click um Power Settings zu oefnen." ;;
	6) setsid -f "$TERMINAL" -e "$EDITOR" "$0" ;;
esac;

echo ""
